apiVersion: v1
kind: Secret
metadata:
  name: anubiswatch-secrets
  namespace: anubiswatch
  labels:
    app.kubernetes.io/name: anubiswatch
type: Opaque
stringData:
  # Required before applying. Leaving CHANGE_ME will fail startup validation.
  admin-password: "CHANGE_ME"
  # Add other secrets here:
  # encryption-key: ""
  # cluster-secret: ""
