# Template only: copy this file outside version control and add stringData
# values from your secret manager before applying. As committed, this Secret
# intentionally contains no keys, so pods fail closed instead of using a
# placeholder admin password.
apiVersion: v1
kind: Secret
metadata:
  name: anubiswatch-secrets
  namespace: anubiswatch
  labels:
    app.kubernetes.io/name: anubiswatch
type: Opaque
# Example rendered values (do not commit real secrets):
# stringData:
#   admin-password: "<strong password>"
#   encryption-key: "<32-byte base64 key>"
#   cluster-secret: "<cluster secret>"
